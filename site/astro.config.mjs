import { defineConfig } from 'astro/config';

export default defineConfig({
  // GitHub Pages로 배포할 때는 아래 두 줄의 주석을 해제하고
  // <사용자명>/<리포이름>에 맞게 값을 채워주세요.
  // site: 'https://<사용자명>.github.io',
  // base: '/rookie-of-the-year',
});
