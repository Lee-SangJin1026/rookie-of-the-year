import { defineConfig } from 'astro/config';

export default defineConfig({
  // GitHub Pages 배포 주소 (https://<사용자명>.github.io/<리포이름>)
  site: 'https://Lee-SangJin1026.github.io',
  base: '/rookie-of-the-year',
});
